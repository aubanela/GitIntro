//here is a text line
int main(char argc, char **argv) {
    bool var = false;
    bool cond = true;
    //encore un commit
    if(cond){
        var = true;
    }
    //commit 
    //nouvel modif
    //commit en plus

    //a merger new (branch du haut) dans clenaing (base)
}

